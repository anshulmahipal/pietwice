# Feature registry

Use **canonical names** below when you ask for changes (“work on **House expense**”, “fix **Pin gate**”). Anyone assisting you can open this file to map your wording to code and tests.

Add a row when you ship a new feature; keep names stable so history stays searchable.

| Canonical name | Also called | What it is | Main code | Tests |
| --- | --- | --- | --- | --- |
| **App shell** | Root layout | Safe area, status bar, wraps app with `PinGate`. BondWallet branding: `app.json` icon/splash (`assets/icon.png`, `assets/bondwallet-logo.png`, cream `#f6f1e8`); **Pin gate** loading card shows the logo. | `App.tsx`, `app.json`, `assets/*`, `src/auth/PinGate.tsx` | `App.test.tsx`, `PinGate.test.tsx` |
| **EAS Update** | OTA updates | JS bundle updates via **`expo-updates`** and EAS; `runtimeVersion.policy: appVersion` ties updates to `expo.version`. Build profiles use **channels** (`production`, `preview`, `development`) in `eas.json`. Publish: `npm run update:production` / `update:preview` (requires `eas login` and a build on that channel). Native or plugin changes need a new store build, not only OTA. | `app.json` (`expo.updates`, `runtimeVersion`, `plugins`), `eas.json`, `package.json` scripts | `—` |
| **Pin gate** | App lock, PIN unlock | Blocks main UI until PIN is set/verified; **first launch**: enter 4-digit PIN then **confirm** on a second screen; invalid keychain values are cleared so setup is not mistaken for unlock. Secure storage; session `replaceStoredPin` for in-app PIN updates. After unlock, **`HouseholdIncomeGate`** runs before tabs (see **Household onboarding**). | `src/auth/PinGate.tsx`, `src/auth/usePinGate.ts`, `src/auth/PinSessionContext.tsx`, `src/auth/pinGateLogic.ts`, `src/auth/pinSecureStorage.ts` | `pinGateLogic.test.ts`, `usePinGate.test.tsx`, `PinGate.test.tsx` |
| **Household onboarding** | Income + budget intro | After PIN: **income** screen (husband/wife monthly, `household_income_setup_v1`) then **budget overview** where the user can **add** (tap **+** to open an alert-style modal), **edit**, or **delete** categories (`removeCategoryAt` keeps at least one row), **Continue** saves to SQLite and sets `household_budget_onboarding_seen_v1`. Same UI as **Expense categories** (`ExpenseCategoriesScreen`). Then main tabs. Home hero shows combined income when present. AsyncStorage keys not in SQLite backup. | `src/householdIncome/HouseholdIncomeGate.tsx`, `src/householdIncome/householdIncomeGateLogic.ts`, `src/screens/HouseholdIncomeSetupScreen.tsx`, `src/screens/HouseholdBudgetOverviewScreen.tsx`, `src/expenseCategories/ExpenseCategoriesScreen.tsx`, `src/expenseCategories/useExpenseCategories.ts`, `src/expenseCategories/expenseCategoryLogic.ts`, `src/householdIncome/householdIncomeStorage.ts`, `src/householdIncome/householdBudgetOnboardingStorage.ts`, `src/householdIncome/useHouseholdIncome.ts` | `householdIncomeGateLogic.test.ts`, `householdIncomeLogic.test.ts`, `householdIncomeStorage.test.ts`, `householdBudgetOnboardingStorage.test.ts`, `HouseholdIncomeSetupScreen.test.tsx`, `HouseholdBudgetOverviewScreen.test.tsx`, `useHouseholdIncome.test.tsx` |
| **Set login PIN** | First-time PIN | Two-step create (`create_pin` → `create_pin_confirm`) using `SetLoginPinScreen`; keypad backspace uses an icon. | `src/screens/SetLoginPinScreen.tsx`, `src/auth/PinGate.tsx` | `SetLoginPinScreen.test.tsx`, `PinGate.test.tsx` |
| **PIN entry** | Keypad logic | Shared logic for entering/validating PIN digits. | `src/pin/pinEntryLogic.ts` | `pinEntryLogic.test.ts` |
| **Main tabs** | Bottom tabs | House expense stack, bills, credit card list, profile tab (`ProfileStack`: home, investments, insurance); Ionicons on each tab (home / receipt / card / person). | `src/navigation/MainTabs.tsx` | `MainTabs.test.tsx` |
| **Bills hub** | Bill organizer, bill calendar | Bills tab for monthly and one-time bills with reminders toggle, add sheet, month calendar and due-day detail modal. SQLite `bills`; local notifications for next reminder trigger. | `src/screens/tabs/BillsScreen.tsx`, `src/bills/*` | `BillsScreen.test.tsx`, `billsLogic.test.ts`, `useBillReminders.test.tsx` |
| **House expense stack** | House expense nav | Stack: home, expense details. **Expense categories** moved to **More** tab. | `src/navigation/HouseExpenseStack.tsx`, `src/navigation/houseExpenseStackTypes.ts` | `HouseExpenseStack.test.tsx` |
| **House expense** | Dashboard, home | Combined spend/budget card (opens details) + expense calendar; dashboard hook; optional **household income** line when onboarding saved. | `src/screens/tabs/HouseExpenseScreen.tsx`, `src/houseExpense/useHouseExpenseDashboard.ts`, `src/houseExpense/expenseDashboardLogic.ts`, `src/houseExpense/expenseDate.ts` | `HouseExpenseScreen.test.tsx`, `useHouseExpenseDashboard.test.tsx`, `expenseDashboardLogic.test.ts`, `expenseDate.test.ts` |
| **House expense detail** | Category breakdown | Per-category bars and spent modal only (no combined total); same dashboard hook as home. | `src/screens/tabs/HouseExpenseDetailScreen.tsx` | `HouseExpenseDetailScreen.test.tsx` |
| **Expense calendar** | Calendar, spending by day | Month calendar in a bordered frame; per-day net amount (digits only); tap day for line items (`expense_line_items`). | `src/screens/tabs/HouseExpenseScreen.tsx`, `src/houseExpense/useExpenseCalendarMonth.ts`, `src/houseExpense/expenseCalendarLogic.ts`, `src/expenseCategories/expenseCategoryDb.ts` | `HouseExpenseScreen.test.tsx`, `useExpenseCalendarMonth.test.tsx`, `expenseCalendarLogic.test.ts` |
| **Expense categories** | Categories, settings | Default categories (with starter monthly budget amounts for new DBs), CRUD-ish DB, shared screen **`ExpenseCategoriesScreen`** (onboarding **Continue** vs More tab **Save**); **More** → Expense categories. Thin wrappers: `HouseholdBudgetOverviewScreen`, `HouseExpenseSettingsScreen`. | `src/expenseCategories/ExpenseCategoriesScreen.tsx`, `src/expenseCategories/*`, `src/screens/tabs/HouseExpenseSettingsScreen.tsx`, `src/screens/tabs/ProfileScreen.tsx`, `src/navigation/ProfileStack.tsx` | `expenseCategoryDb.test.ts`, `expenseCategoryLogic.test.ts`, `useExpenseCategories.test.tsx`, `HouseExpenseSettingsScreen.test.tsx`, `HouseholdBudgetOverviewScreen.test.tsx`, `ProfileScreen.test.tsx` |
| **Credit card list** | Cards tab, credit dashboard | Summary above calendar shows registered count; tap opens full card list modal. Optional bill day reminders (toggle): monthly local notification at 9:00 on each card’s company bill day (`expo-notifications`). Header Add opens add sheet; calendar highlights bill dates; tap day for due-detail modal. SQLite `credit_cards`. | `src/screens/tabs/CreditCardListScreen.tsx`, `src/creditCards/creditCardDb.ts`, `src/creditCards/creditCardCalendarLogic.ts`, `src/creditCards/creditCardLogic.ts`, `src/creditCards/creditCardReminderLogic.ts`, `src/creditCards/creditCardReminders.ts`, `src/creditCards/useCreditCardReminders.ts`, `src/creditCards/useCreditCards.ts`, `App.tsx` | `creditCardDb.test.ts`, `creditCardCalendarLogic.test.ts`, `creditCardLogic.test.ts`, `creditCardReminderLogic.test.ts`, `creditCardReminders.test.ts`, `useCreditCardReminders.test.tsx`, `useCreditCards.test.tsx`, `CreditCardListScreen.test.tsx` |
| **Profile stack** | Profile navigation | Native stack: **More** home, **Expense categories**, **Insights** hub, **Accounts** hub, **Budgets** hub, **Investments** hub, **Insurance** hub (stack headers; tab `headerShown: false`). | `src/navigation/ProfileStack.tsx`, `src/navigation/profileStackTypes.ts` | `ProfileScreen.test.tsx` |
| **Profile** | Account home | **Finance**: rows open insights / accounts / budgets / investment / insurance hubs. **Data** (native): export / restore full SQLite backup. **Security**: change entry PIN (verify → new → confirm; modal + keypad); updates secure store and session ref. | `src/screens/tabs/ProfileScreen.tsx`, `src/auth/useChangePinFlow.ts` | `ProfileScreen.test.tsx`, `useChangePinFlow.test.tsx` |
| **Data backup** | Export DB, restore after reinstall | Serializes shared `monthly_expense.db` (`SQLite.serializeAsync`); share sheet writes a `.db` file; restore picks a file, replaces DB via deserialize + `backupDatabaseAsync`, then user must fully restart the app. PIN (`expo-secure-store`), **household onboarding** income + budget flags (`AsyncStorage`), and bill/card reminder toggles (`AsyncStorage`) are not in the backup file. | `src/backup/*`, `src/database/monthlyExpenseSqliteDb.ts`, `src/screens/tabs/ProfileScreen.tsx` | `backupLogic.test.ts`, `monthlyExpenseBackup.test.ts`, `ProfileScreen.test.tsx` |
| **Accounts hub** | Ledger, cashflow | Manual accounts (bank/cash/wallet/credit card), running balances, add income/expense entries, month cashflow summary and recent ledger list. SQLite `accounts` + `account_entries`. | `src/screens/tabs/AccountsHubScreen.tsx`, `src/accounts/*` | `AccountsHubScreen.test.tsx`, `accountLogic.test.ts`, `useAccountsLedger.test.tsx` |
| **Budget hub** | Budget planner, limits | Period budgets (weekly, bi-weekly, monthly), live spent totals from expense line items, status states (on track/near/over/critical), modal editor per period. | `src/screens/tabs/BudgetHubScreen.tsx`, `src/budgets/*`, `src/expenseCategories/expenseCategoryDb.ts` | `BudgetHubScreen.test.tsx`, `budgetLogic.test.ts`, `useBudgetsOverview.test.tsx` |
| **Insights hub** | Finance dashboard, India summary | This-month and FY-to-date metrics, upcoming 30-day bill obligations, budget risk ranking; values formatted in INR. | `src/screens/tabs/InsightsHubScreen.tsx`, `src/insights/useInsightsOverview.ts`, `src/localization/indiaFormat.ts` | `InsightsHubScreen.test.tsx`, `useInsightsOverview.test.tsx`, `indiaFormat.test.ts` |
| **Finance hub layout** | Shared finance shell | Scroll + caption, summary tile (count, tap opens list on hubs), bordered month calendar with optional `markedDates`, month swipe, and day press. Uses `financeCalendarTheme`. | `src/financeHub/FinanceHubLayout.tsx`, `src/financeHub/monthlyDayCalendarLogic.ts`, `src/financeHub/monthlyDayInputLogic.ts` | `FinanceHubLayout.test.tsx`, `monthlyDayCalendarLogic.test.ts`, `monthlyDayInputLogic.test.ts` |
| **Investment hub** | Investments | SQLite `investments` (holding name + monthly activity day); Profile entry: count, calendar dots, header **Add** sheet, summary list modal, day-detail modal. | `src/screens/tabs/InvestmentHubScreen.tsx`, `src/investments/*`, `src/financeHub/financeEntityHubModalStyles.ts` | `InvestmentHubScreen.test.tsx`, `investmentDb.test.ts`, `investmentLogic.test.ts`, `useInvestments.test.tsx`, `ProfileScreen.test.tsx` |
| **Insurance hub** | Insurance | SQLite `insurance_policies` (policy name + reminder/renewal day); same hub UX as investments with policy copy. | `src/screens/tabs/InsuranceHubScreen.tsx`, `src/insurancePolicies/*` | `InsuranceHubScreen.test.tsx`, `insurancePolicyDb.test.ts`, `insurancePolicyLogic.test.ts`, `useInsurancePolicies.test.tsx`, `ProfileScreen.test.tsx` |

## How to use with an assistant

- Name a feature from the **Canonical name** column (or a close **Also called** phrase).
- Say “read `FEATURES.md`” if the session is new so the registry stays in sync with your request.
